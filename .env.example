# URL used by the browser to reach the backend.
VITE_API_BASE_URL=http://localhost:8080

# Frontend origin allowed by the backend CORS policy.
QUARKUS_HTTP_CORS_ORIGINS=http://localhost:5173

# In-memory quiz cache settings.
QUIZ_CACHE_TTL=1h
QUIZ_CACHE_MAXIMUM_SIZE=10000
